# TEKMOVELA TypeScript SDK (skeleton)

Planned surface:

- Web/Node agent harness for browser-driven testing.
- UI Structured Failure Evidence capture (`screenshot + DOM + alert`, the `ui-repair/v1` profile).
- Trace adapter for emitting TraceEnvelope perspectives.

Nothing is implemented yet; the package metadata is in place.
